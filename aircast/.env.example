# AirCast runtime env — copy to `.env` on the CT if you need to override defaults.
TZ=Pacific/Auckland
# Pin the image instead of tracking latest, if you want manual control.
# AIRCAST_IMAGE=1activegeek/airconnect:1.10.1
