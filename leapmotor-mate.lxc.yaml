# Leapmotor Mate — self-hosted TeslaMate-style companion for the C10.
#
# Docker-only app (no community-scripts ct/leapmotor-mate.sh, no native install),
# so — exactly like Media/youtarr — this CT is a thin Docker host: app: docker →
# ct/docker.sh installs Docker Engine, then our self-contained compose
# (leapmotor-mate/compose.yml) is layered on. The upstream image is
# self-contained (only `image:` pulls, only a data-dir volume) so we ship a
# minimal compose + .env instead of cloning the repo. See README.md for the
# layer-on steps. Run the stack from /opt/leapmotor so compose's ./data + ./certs
# resolve to the CT local disk.
#
# Docker needs nesting (and keyctl) on an unprivileged LXC — declared below;
# ct/docker.sh also sets var_nesting itself on create.
#
# NO NFS MOUNT. All state (SQLite DB, uploaded certs, secret key) lives on the CT
# local disk under /opt/leapmotor/data — a few MB of telemetry. So unlike the
# *arr/youtarr there is no volume4 dependency and no pre-start mount guard.
#
# EXPOSURE — INTERNAL-ONLY (no tunnel ingress). Reached over VLAN 1040 only (the
# IoT VLAN, co-located with the SmartHome MQTT broker); it stores the dedicated
# Leapmotor account credentials, so it stays off the public net (set
# MATE_AUTH_PASSWORD in .env regardless). To expose later, add a cloudflared
# member to this stack (+ dependsOn) and gate with CF Access.
# Validate against ../../Infrastructure/schema/shape.schema.json.

# ⚠️ RETIRED — superseded by CT 6004 (SmartHome podman host) (#285). The guest is STOPPED and tagged
# `retired` in Proxmox, awaiting a manual delete decision. The tag is declared below so a
# converge does not strip it (CtConfigReconciler reconciles tags and would treat it as drift).
# DELETE THIS SHAPE once the guest is destroyed.
apiVersion: homelab/v1
kind: LXC

metadata:
  name: leapmotor-mate
  stack: SmartHome
  tags: [leapmotor, car, ev, docker, retired]   # RETIRED — replaced by CT 6004 (SmartHome podman host), #285

spec:
  # RETIRED — Mate runs on CT 6004 (SmartHome podman host) as a quadlet, #285. CT 4100 is
  # already deleted, which is the dangerous state rather than the safe one: absent looked
  # identical to never-created, so the next full converge of this stack would have rebuilt
  # it. That is exactly what happened to Media's CT 5113 (#362) — a `retired` tag and a
  # comment stopped nothing, because the engine read neither. Converge now refuses to
  # create or write this member, and `--only` cannot override it.
  manage: retired
  app: docker            # community-scripts slug → ct/docker.sh (Docker Engine host)
  ctid: 4100             # ADOPTED IN-PLACE, out-of-block: SmartHome's range is 6000–6099, but this CT
                         # is already live at 4100 — kept to avoid a redeploy. Documented exception,
                         # mirroring the adopted HA VM 2000 (see stack.yaml + README). Renumbering to
                         # a 6000-block id would mean recreating the container.
  cores: 1               # light: FastAPI web + a single polling loop
  memory: 1024           # MB — Docker daemon + one Python container + SQLite
  disk: 10               # GB — Docker image layers + tiny SQLite store under /opt/leapmotor

  features:
    nesting: true        # required: Docker-in-LXC
    keyctl: true         # Docker/containerd keyring ops on unprivileged LXC

  # node / network / os / source / timezone inherited from stack.yaml defaults.
  # No mounts, no hookscript — state is CT-local (see header).
